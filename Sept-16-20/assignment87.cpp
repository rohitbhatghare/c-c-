#include <iostream>
#include <string>

using namespace std; 	//introduces namespace std
int main()
{
int x,y,z;
    cout<<"Enter the X"<<endl;
    cin>>x;
    cout<<"Enter the Y"<<endl;
    cin>>y;
    cout<<"Enter the Y"<<endl;
    cin>>z;








if((x < y) && (y < z))
    {
        cout<<"Possible";

    }
    else

        cout<<"Not Possible";
    return 0;
}

